/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com',
      'travelchapes.s3.eu-north-1.amazonaws.com',
    ],
  },
}

export default nextConfig
